# Statics Determinacy Feature

This directory is designated for feature-specific modules related to structural determinacy and static stability in future project stages.
Currently, the validation rules live in `src/lib/domain/validation/checker.ts` and the UI diagnostics view is in `src/lib/ui/FeedbackPanel.svelte`.
