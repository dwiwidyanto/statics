# Statics FBD Feature

This directory is designated for feature-specific components, custom hooks, or stores related to the 2D Free-Body Diagram Builder in future project stages.
Currently, the core SVG rendering lives in `src/lib/ui/FbdCanvas.svelte` and the orchestrator is in `src/app/pages/PracticePage.svelte`.
