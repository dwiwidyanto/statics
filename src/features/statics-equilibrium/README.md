# Statics Equilibrium Feature

This directory is designated for feature-specific modules related to rigid-body equilibrium in future project stages.
Currently, the core domain calculations live in `src/lib/domain/solvers/equilibrium.ts` and the UI view is in `src/lib/ui/EquationsView.svelte`.
