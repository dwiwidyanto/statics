# Learning Content Feature

This directory is designated for feature-specific modules related to learning materials and interactive modules in future project stages.
Currently, the topics database lives in `src/content/topics/statics-content.ts` and the UI view is in `src/app/pages/ConceptPage.svelte`.
