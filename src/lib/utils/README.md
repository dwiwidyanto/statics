# Utilities Directory

This directory is designated for general helper functions, formatting utilities, or common tools used across the application.
Currently, formatting and conversions live in `src/lib/domain/geometry/vector2d.ts` and `src/lib/domain/solvers/equilibrium.ts`.
